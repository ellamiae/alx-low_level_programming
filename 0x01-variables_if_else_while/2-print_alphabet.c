#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Description: alphabet game
 *
 * Return: 0 (success)
*/
int main(void)
{	char i;
	for (i = 'a' ; i <= 'z' ; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
